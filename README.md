# DevTracker

A command-line task manager built in Java.

## How to run
Compile and run CLI.java from the src/ directory.

## Commands
- add — create a new task
- list — view all tasks sorted by priority
- complete — mark a task as done
- exit — quit the app